# ================================
# MySQL Database Configuration
# ================================
spring.datasource.url=jdbc:mysql://localhost:3306/uber_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Hari@123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

server.port=8081

# ================================
# Google Maps API
# ================================
google.api.key=${GOOGLE_API_KEY}

# ================================
# Pricing Config
# ================================
pricing.base-fare=50.00
pricing.per-km=10.00

# ================================
# REST Client Timeouts
# ================================
rest.client.connect-timeout-ms=5000
rest.client.read-timeout-ms=10000

# ================================
# Kafka Config
# ================================
spring.kafka.bootstrap-servers=localhost:9093

spring.kafka.consumer.group-id=user-service
spring.kafka.consumer.auto-offset-reset=earliest
spring.kafka.consumer.key-deserializer=org.apache.kafka.common.serialization.StringDeserializer
spring.kafka.consumer.value-deserializer=org.springframework.kafka.support.serializer.JsonDeserializer

spring.kafka.producer.key-serializer=org.apache.kafka.common.serialization.StringSerializer
spring.kafka.producer.value-serializer=org.springframework.kafka.support.serializer.JsonSerializer

spring.kafka.properties.spring.json.trusted.packages=*
# logging.level.org.apache.kafka=WARN
# logging.level.org.springframework.kafka=WARN
